<div class="container">
  <div class="row">
    <div class="col-lg">
      <h3 class="text-center pt-3 pb-5"><%= @tag.tag_name+"　タグ投稿一覧" %></h3>
        <% if @posts.present? %>
        <table>
          <% @posts.each do |tag_post| %>
            <tr class="p-3">
                <td class="m-3 p-3"><%= link_to image_tag(tag_post.get_image(200, 200)), post_path(tag_post) %></td>
                <td class="col-3 px-5"><%= link_to tag_post.stone_name, post_path(tag_post) %></td>
                <td class="px-5"><%= tag_post.caption %></td>
            </tr>
          <% end %>
        </table>
        <% else %>
        <div class="text-center text-danger h5">
           ※　"<strong><%= @tag.tag_name %></strong>"　に紐づく投稿が見つかりませんでした。
        </div>
        
        <% end %>
    </div>
  </div>
</div>

