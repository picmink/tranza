<h3>全ユーザー一覧</h3>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, admin_comments(@comments) %> - <%= user.email %>
      <%= link_to '削除', admin_user_path(user), method: :delete, data: { confirm: '本当に削除しますか？' } %>
    </li>
  <% end %>
</ul>