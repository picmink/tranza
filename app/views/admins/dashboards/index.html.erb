
</ul>
<div container>
  <div class="row">
    <div class="col-lg">
      
      
      <h3>全ユーザー一覧</h3>
<ul class="navbar-nav ml-auto">
  <% @users.each do |user| %>
    <li class="p-4">
      <strong><%= user.name %></strong> - <%= user.email %>
      <% user.comments.each do |comment| %>
        <div>
          <%= link_to comment.comment, admins_comment_path(comment.id), method: :delete, data: { confirm: '本当に削除しますか？' },  class: "text-danger " %>
        </div>
      <% end %>
      <%= link_to '削除', admins_user_path(user), method: :delete, data: { confirm: '本当に削除しますか？' }, class: "btn btn-nav-link text-danger " %>
    </li>
  <% end %>
</ul>


    </div>
  </div>
</div>