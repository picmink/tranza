<div>

    <%= link_to image_tag(@user.get_profile_image(200, 200)), edit_user_path(@user) %>
    <h10><%= link_to @user.name, edit_user_path(@user) %></h10>

 
    <h8>投稿一覧</h8>
        <table>
          <% @posts.each do |post| %>
            <tr>
              <%= link_to image_tag(post.get_image(150, 150)), post_path(post.user_id) %>
              <%= link_to post.stone_name, post_path(post.user_id) %>
              <%= post.caption %>
            </tr>
          <% end %>
        </table>
    <%= link_to "投稿する", new_post_path %>
    
    <h8>コメント一覧</h8>
        <table>
            
        </table>
    
</div>
    